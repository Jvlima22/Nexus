---
tipo: diario
data: 2026-06-06
tags:
  - registro/diario
---

# 2026-06-06

## O que foi feito
- **Fase 7 — Autotrader determinístico** implementado: robô autônomo que ocupa o lugar do
  par OpenClaw+IA no laço de decisão, **sem LLM e sem custo de token**. `connector/autotrader.py`
  (instância `engine`): thread que a cada 60s, por ativo da watchlist, roda `indicators.analyze`
  (M5), exige **confluência** com M15 (mesma direção, usa a menor confiança), dimensiona o
  stake em **2% da banca** e manda pra `orders.place_order(..., confidence)`. O **Risk Judge
  segue sendo a porta inegociável** — o robô não duplica nenhuma regra de risco.
- `config.py`: bloco `AUTOTRADER_*` (enabled, assets, timeframe/confirm_tf, confluência, poll,
  candles, expiração, option_type, cooldown) + `autotrader_assets_list`.
- `main.py`: `engine.start()` no lifespan + `GET /autotrader/status` e `POST /autotrader/toggle`
  (kill switch em runtime, sem reiniciar). `.env.example` documentado.
- **Segurança:** `AUTOTRADER_ENABLED=false` por padrão; modo PRACTICE. Cooldown por ativo
  evita metralhar sinal e re-vetar todo tick; posição aberta segura o ativo até expirar.
- Smoke: `py_compile` limpo nos 3 arquivos; `import autotrader` OK; `engine.status()` retorna
  estado coerente (enabled=False, EURUSD, M5/M15, confluência, stake 2%).
- Ver [[2026-06-06 Autotrader deterministico Fase 7]].
- **UI do Autotrader** na rota `/mercado`: `src/components/ai/AutotraderPanel.tsx` (kill switch
  liga/desliga via `POST /autotrader/toggle`, badge PRACTICE/REAL, watchlist/TF/stake, ticks e
  feed das últimas decisões com auto-refresh 10s) + tipos/funções `fetchAutotraderStatus`/
  `toggleAutotrader` em `connector.ts`. Montado acima do `SignalPanel`. `tsc --noEmit` limpo.
- **Scanner multi-moeda:** robô deixou de vigiar 1 par fixo → **descobre ativos abertos
  dinamicamente**. `config.py` (`AUTOTRADER_SCAN_OPEN/UNIVERSE/MIN_PAYOUT/MAX_ASSETS/
  ASSETS_REFRESH_S/MAX_OPEN`), `autotrader.py` (`_is_currency_pair` + `_ISO_CCY`, `_watchlist`
  com cache lendo `supabase_sync.get_open_assets`, trava `max_open` via `get_open_nexus_trades`),
  `supabase_sync.get_open_assets`. UI mostra contagem da watchlist + universo + máx posições.
  Validado ao vivo: **30 pares de moeda -OTC** varridos (cripto/ação/índice excluídos), 1
  `vetoed OUTSIDE_SESSION`. `tsc` + `py_compile` limpos. Connector reiniciado (PID 34652).
  Ver [[2026-06-06 Autotrader deterministico Fase 7]] (seção "varredura multi-ativo").
- **Backtest empírico do OTC** (`connector/_backtest.py`, 9.100 sinais reais, 30 pares,
  exp 5min, payout 84% → breakeven 54,3%): estratégia atual em OTC ≈ **50% de acerto
  (abaixo do breakeven → perde)**; confiança não discrimina; `conf≥0.8` nunca ocorre;
  confluência melhora pouco (51,7% no melhor caso); **autocorrelação ≈ 0 → OTC é random
  walk** (preço sintético da corretora). Conclusão: **sem edge técnico no OTC** com a v1.
  Relatório de decisões também via `connector/_report.py` (risk_events: 73 negadas —
  45 OUTSIDE_SESSION, 28 MACRO_CONFLICT; 0 ordens reais do robô).
- **To-do do Robô OTC v2** criado: [[Robo OTC - To Do]] (gate de evidência/backtest —
  só opera par com acerto medido > 57%; validação em PRACTICE antes de REAL). Trabalho
  pausado a pedido — retomar depois.


## Tocado hoje (auto-log)
- 20:02 `[Edit]` `connector\config.py`
- 20:02 `[Edit]` `connector\config.py`
- 20:03 `[Write]` `connector\autotrader.py`
- 20:03 `[Edit]` `connector\main.py`
- 20:03 `[Edit]` `connector\main.py`
- 20:03 `[Edit]` `connector\main.py`
- 20:16 `[Edit]` `src\lib\connector.ts`
- 20:17 `[Write]` `src\components\ai\AutotraderPanel.tsx`
- 20:17 `[Edit]` `src\routes\mercado.tsx`
- 20:17 `[Edit]` `src\routes\mercado.tsx`
- 21:11 `[Edit]` `connector\supabase_sync.py`
- 21:11 `[Edit]` `connector\config.py`
- 21:14 `[Edit]` `connector\autotrader.py`
- 21:14 `[Edit]` `connector\autotrader.py`
- 21:14 `[Edit]` `connector\autotrader.py`
- 21:15 `[Edit]` `connector\autotrader.py`
- 21:15 `[Edit]` `connector\autotrader.py`
- 21:15 `[Edit]` `connector\autotrader.py`
- 21:16 `[Edit]` `connector\.env.example`
- 21:16 `[Edit]` `src\lib\connector.ts`
- 21:17 `[Edit]` `src\components\ai\AutotraderPanel.tsx`
- 21:17 `[Edit]` `src\components\ai\AutotraderPanel.tsx`
- 21:19 `[Edit]` `src\components\ai\AutotraderPanel.tsx`
- 21:37 `[Write]` `connector\_report.py`
- 21:38 `[Write]` `connector\_report.py`
- 21:40 `[Edit]` `connector\_report.py`
- 21:52 `[Write]` `connector\_backtest.py`


## DecisÃµes
- [[2026-06-06 Autotrader deterministico Fase 7]] — laço determinístico reusando `indicators`
  + Risk Judge; coexiste com o caminho OpenClaw+IA (mesmo juiz). Stake = 2% da banca,
  confluência M5+M15, default desligado/PRACTICE.


## ExclusÃµes


## Bloqueios / pendÃªncias
- **Validar em PRACTICE** ponta a ponta: subir o connector, ligar via
  `POST /autotrader/toggle {enabled:true}` e observar `GET /autotrader/status`.
- Reiniciar o connector p/ carregar as novas envs `AUTOTRADER_*`.
- UI no front pra ligar/desligar e ver as decisões do robô (hoje só via API) — pendente.
- Decidir watchlist real e expiração por ativo antes de qualquer cogitação de REAL.


## Aprendizados
- O robô "que se comporta como OpenClaw+IA" não precisava de IA: a interpretação já era
  determinística (`indicators.analyze`, `source="rules"`). O laço foi só o agendador que
  faltava ligando análise → execução, com o Risk Judge intacto como autoridade final.

