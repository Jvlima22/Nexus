---
tipo: diario
data: 2026-05-24
tags:
  - registro/diario
---

# 2026-05-24

## O que foi feito
- Reativada a função de **upload de arquivo / link de URL** no Knowledge, agora
  alimentando o **contexto da IA via RAG** (estratégia Prompt Caching — só tokens Claude).
- Criado: migration `knowledge_sources`, client `src/lib/anthropic.ts`, server fns
  `ingestKnowledge`/`askNexus`, componente `KnowledgeContext.tsx` e aba "Contexto da IA".
- Dep nova `unpdf` (PDF no Worker); env nova `ANTHROPIC_API_KEY`.
- **Integração OpenClaw (Fase 1, read-only):** OpenClaw já estava instalado (v2026.3.28).
  Criada skill `nexus_trader` (análise read-only, nunca opera), seção NEXUS no `TOOLS.md`,
  loop no `HEARTBEAT.md` (desativado por padrão), e endpoint **`POST /vault/note`** no
  Connector (escrita sandbox em `30_Trading/**`,`40_Registros/**`). Ferramenta: [[OpenClaw]].
- **Descoberta:** o cérebro do OpenClaw está em **OpenAI GPT-5.4**, não Claude → decisão de
  cérebro pendente.

## Tocado hoje (auto-log)
- 16:23 `[Write]` `src\lib\anthropic.ts`
- 16:24 `[Write]` `src\lib\rpc\knowledge.ts`
- 16:25 `[Write]` `src\components\ai\KnowledgeContext.tsx`
- 16:26 `[Edit]` `src\routes\knowledge.tsx`
- 16:27 `[Edit]` `src\routes\knowledge.tsx`
- 16:27 `[Edit]` `src\routes\knowledge.tsx`
- 16:28 `[Edit]` `package.json`
- 16:28 `[Edit]` `.dev.vars`
- 16:28 `[Edit]` `.env.example`
- 16:29 `[Edit]` `src\lib\rpc\knowledge.ts`
- 17:25 `[Write]` `scripts\verify-knowledge.mjs`
- 17:33 `[Write]` `scripts\verify-anthropic.mjs`
- 18:46 `[Edit]` `connector\vault.py`
- 18:46 `[Edit]` `connector\main.py`
- 18:47 `[Write]` `C:\Users\TGL Solutions\.openclaw\workspace\skills\nexus_trader\SKILL.md`
- 18:47 `[Edit]` `C:\Users\TGL Solutions\.openclaw\workspace\TOOLS.md`
- 18:48 `[Write]` `C:\Users\TGL Solutions\.openclaw\workspace\HEARTBEAT.md`


## Decisões
- [[2026-05-24 RAG do Knowledge - Prompt Caching]] — RAG por Prompt Caching no v1,
  com `tsv`/GIN já cabeados para evoluir a FTS (Fase 2).

## Exclusões


## Bloqueios / pendências
- **Pendente do usuário:** rodar `npm install` (para `unpdf`) e aplicar a migration no
  SQL Editor do Supabase. (`ANTHROPIC_API_KEY` já preenchida e validada.)
- **BLOQUEIO de billing:** chave Anthropic é válida, mas a conta está **sem crédito de
  API** (testado direto: HTTP 400 "credit balance is too low"). Comprar crédito em
  console.anthropic.com → Plans & Billing (~US$ 5 já basta com prompt caching).
- **Plano Pro NÃO serve:** Claude Pro/Max e Claude Code não dão acesso à API nem
  compartilham saldo — billing separado. Ver [[2026-05-23 Custo da IA - API pay-as-you-go vs assinatura]].
- O `401` visto no app era chave desatualizada na memória do dev → **reiniciar
  `npm run dev`** após editar `.dev.vars`.
- **DECISÃO (OpenClaw):** cérebro = **GPT-5.4 agora** (já configurado, sem bloqueio
  Anthropic); migrar p/ Claude quando houver crédito de API. Ver [[OpenClaw]].
- **BLOQUEIO confirmado nos DOIS provedores:** ao testar a skill via `openclaw agent`,
  OpenAI retornou **"You exceeded your current quota"** → conta OpenAI sem cota. Somado
  ao Anthropic sem crédito, o cérebro NÃO roda até pôr saldo em um dos dois. Todo o
  pipeline (Connector, gateway, skill, `/vault/note`) está montado e ok — falta só billing.
- Nota: `openclaw agent` exige `--agent main`; sem pareamento o CLI cai em modo embedded
  (ainda chega a chamar o modelo). Parear depois com `openclaw devices`/`pairing`.

## Aprendizados
- A Anthropic **não oferece endpoint de embeddings** — "RAG só com tokens Claude" se
  resolve com Prompt Caching (ou FTS), não com vetores.
- **Claude Pro/Max ≠ API Anthropic**: a assinatura não pode alimentar chamadas
  programáticas do app; a API tem billing pré-pago separado.

